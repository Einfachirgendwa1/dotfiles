(defsrc
  caps
  ;
  ret
  grv
  lctrl
)

(deflayer base
  lctrl           ;; Capslock -> Left Control
  ret             ;; ö -> Return
  ;               ;; Return -> ö
  (multi lctrl b) ;; ^ -> Ctrl+B (tmux)
  bspc            ;; Ctrl -> Delete
)
