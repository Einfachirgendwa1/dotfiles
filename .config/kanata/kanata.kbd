(defsrc
  caps
  ;
  ret
  lctrl
)

(deflayer base
  lctrl           ;; Capslock -> Left Control
  ret             ;; ö -> Return
  ;               ;; Return -> ö
  bspc            ;; Ctrl -> Delete
)
