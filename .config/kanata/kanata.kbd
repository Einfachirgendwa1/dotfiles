(defsrc
  caps
  ;
  ret
  grv
  bspc
  lmet
  lctrl
)

(deflayer base
  lctrl           ;; Capslock -> Left Control
  ret             ;; ö -> Return
  ;               ;; Return -> ö
  (multi lctrl b) ;; ^ -> Ctrl+B (tmux)
  lmet            ;; Delete -> Windows Key
  (multi lctrl c) ;; Windows Key -> Ctrl+C
  bspc            ;; Ctrl -> Delete
)
